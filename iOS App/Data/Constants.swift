import Foundation
import SwiftUI

enum Constants {
    
    enum Colors {
        static let BACKGROUND = Color(hex: "242529")
        static let FOREGROUND = Color(hex: "DDDDDD")
        static let FIRST_ACCENT = Color(hex: "F4A37E")
        static let SECOND_ACCENT = Color(hex: "A6ABFF")
    }
    
    enum Fonts {
        static let REGULAR = "MavenPro-Regular"
        static let MEDIUM = "MavenPro-Medium"
        static let BOLD = "MavenPro-Bold"
    }
    
}
